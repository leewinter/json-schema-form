import { create } from '@storybook/theming/create';

export default create({
  base: 'dark',
  brandTitle: 'Json Schema Form Library',
  brandUrl: 'https://leewinter.org/',
  brandTarget: '_self',
});
